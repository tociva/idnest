export {
  renderAutoConsentRedirect,
  permissionForScope,
  renderAccessDenied,
  renderConsent,
  renderError,
  renderLogin,
  renderPrivacy,
  renderSettings,
  renderTerms,
} from "./pages";
export type {
  AutoConsentReason,
  ErrorViewModel,
  LoginViewModel,
  SettingsViewModel,
} from "./pages";
