export {
  permissionForScope,
  renderAccessDenied,
  renderConsent,
  renderContinue,
  renderError,
  renderLogin,
  renderPrivacy,
  renderSettings,
  renderTerms,
} from "./pages";
export type { ErrorViewModel, LoginViewModel, SettingsViewModel } from "./pages";
